import React, { ReactElement } from "react";

interface Props {}

export default function AlbumList({}: Props): ReactElement {
  return (
    <div>
      {/* Search */}
      {/* Sorting */}
      {/* Album List */}
    </div>
  );
}
